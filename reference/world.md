# World coastline

A matrix of longitude/latitude coordinates representing a simplified
world coastline, derived from the maps package. Continent boundaries are
separated by `NA` rows. Longitudes are constrained to `abs(lon) <= 180`.

## Usage

``` r
world
```

## Format

A two-column numeric matrix (longitude, latitude)
