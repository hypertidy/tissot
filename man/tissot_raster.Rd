% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tissot_raster.R
\name{tissot_raster}
\alias{tissot_raster}
\alias{image.tissot_raster}
\alias{plot.tissot_raster}
\title{Compute distortion surfaces on a regular grid}
\usage{
tissot_raster(
  target,
  extent = NULL,
  radius = 2e+07,
  nx = 100L,
  ny = NULL,
  metrics = c("scale.area", "angle_deformation", "scale.h", "scale.k"),
  ...,
  source = "EPSG:4326"
)

\method{image}{tissot_raster}(x, metric = NULL, col = NULL, asp = 1, main = NULL, ...)

\method{plot}{tissot_raster}(x, ...)
}
\arguments{
\item{target}{target projection CRS string}

\item{extent}{numeric length 4: \code{c(xmin, xmax, ymin, ymax)} in projected
coordinates. If \code{NULL} (default), estimated from the global lonlat
bounding box (clamped by \code{radius}).}

\item{radius}{maximum half-width of the grid extent in projected units
(default \code{2e7}). The auto-detected extent is clamped so that no
dimension exceeds \code{2 * radius}. Prevents blow-up for projections like
stereographic or gnomonic that map the whole globe to huge coordinates.
Ignored if \code{extent} is supplied.}

\item{nx}{integer; number of grid cells in x (default 100)}

\item{ny}{integer; number of grid cells in y. If \code{NULL}, set to maintain
approximately square cells.}

\item{metrics}{character vector of column names from \code{\link[=tissot]{tissot()}} output to
include as layers. Default includes areal scale, angular deformation,
and meridional/parallel scale factors.}

\item{...}{passed to \code{\link[=image]{image()}}}

\item{source}{source CRS (default \code{"EPSG:4326"})}

\item{x}{a \code{tissot_raster} (from \code{\link[=tissot_raster]{tissot_raster()}})}

\item{metric}{which metric to plot (default: first available)}

\item{col}{colour palette (default: \code{hcl.colors(64, "YlOrRd", rev = TRUE)})}

\item{asp}{aspect ratio (default 1)}

\item{main}{plot title (default: metric name)}
}
\value{
A \code{tissot_raster} list with components:
\describe{
\item{\code{x}}{numeric vector of x (easting) cell centers}
\item{\code{y}}{numeric vector of y (northing) cell centers}
\item{\code{z}}{named list of \verb{ny x nx} matrices, one per metric}
\item{\code{target}}{the target CRS string}
\item{\code{extent}}{the \code{c(xmin, xmax, ymin, ymax)} used}
}
The object has an \code{\link[=image]{image()}} method for quick plotting.
}
\description{
Create a raster-like grid of Tissot distortion metrics in the target
projection. The grid is laid out in projected coordinates, back-projected
to longitude/latitude, and passed through \code{\link[=tissot]{tissot()}}. Returns a list of
matrices compatible with \code{\link[=image]{image()}} and easily converted to raster objects
(e.g. via \code{\link[terra:rast]{terra::rast()}}).
}
\details{
If \code{extent} is \code{NULL}, the function projects the four corners and midpoints
of the \eqn{[-180, 180] \times [-85, 85]}{[-180,180] x [-85,85]} lonlat
bounding box into the target CRS, and uses the resulting range (padded by
5\%) as the grid extent.
}
\examples{
tr <- tissot_raster("+proj=robin", nx = 60)
image(tr)

## specific metric
image(tr, metric = "angle_deformation")

## with coastline overlay
image(tr)
tissot_map()

## polar stereo — use radius to cap extent
tp <- tissot_raster("+proj=stere +lat_0=-90", radius = 5e6, nx = 60)
image(tp)
tissot_map()
}
\seealso{
\code{\link[=tissot]{tissot()}}, \code{\link[=plot.tissot_raster]{plot.tissot_raster()}}, \code{\link[=image.tissot_raster]{image.tissot_raster()}}
}
